# Copy this file to `.dev.vars` for local development (`wrangler dev`).
# `.dev.vars` is git-ignored — NEVER commit real values.
# Production uses:  npx wrangler secret put ADMIN_TOKEN
ADMIN_TOKEN=pick-a-long-local-test-token
IP_HASH_SALT=local-test-salt
